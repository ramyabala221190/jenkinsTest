version: '3.7'
services:
   nginx:
     container_name: nginx-container
     image: ${image-name}:${image-tag}
     build:
        context: .
        dockerfile: Dockerfile
     ports:
     - "8082:80"
     - "8083:80"


